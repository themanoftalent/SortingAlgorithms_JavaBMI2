#include <stdio.h>

int main() {

    int lines;


    printf("Satir Giriniz: ");
    scanf("%d", &lines);


    for (int i = lines; i >= 1; i--) {
        for (int j = 1; j <= i; j++) {
            printf("%d ", j);
        }
        printf("\n");
    }

    return 0;
}
